name: metadrive
dataset:
  # 指向我们在 data_loader.py 里写的函数
  _target_: data_loader_park.build_parking_dataset
  # 【注意】修改为你的绝对路径
  data_path: "/home/lcj/metadrive/metadrive/examples/parking_dataset/parking_dataset_20260202_102510"

# DataLoader 的工作线程数，本地跑建议设小一点，防止卡死
num_workers: 4
decoder_path: null